#include <stdio.h>
#include <math.h>

int main ()
{
    int x;
    printf("enter you number");
    scanf("%d", &x);
    printf("%d", x%2 == 0); // that means when X%2 = 0 then equal 0 so show 1 means true
    return 0;
}
